export const rblxAutoKicker = (value: string): string => `rblx-kicker:${value}`
export const rblxDownloadCounter = (value: string): string =>
  `rblx-downloader:${value}`
export const selfDestructMessage = (message_id: string): string =>
  `self-destruct:${message_id}`
