name: EDis Bot

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  #############
  # Compiling #
  #############
  compile-bot:
    runs-on: ubuntu-20.04

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
          cache: 'yarn'
      - run: yarn
      - name: Compile bot
        run: |
          yarn workspace @sib-edis/bot build
      #################
      # Docker Builds #
      #################
      - name: Login to Packages Container registry
        # if: github['ref'] == 'refs/heads/main'
        uses: docker/login-action@v1.10.0
        with:
          registry: ghcr.io
          username: pacificocean-bot
          password: ${{ secrets.CR_PAT }}
      - name: Build Docker Image
        # if: github['ref'] == 'refs/heads/main'
        run: |
          docker build --pull -f "packages/bot/build/Dockerfile" -t ghcr.io/siberianmh/edis-bot:${{ github.event.pull_request.head.sha }} .
          docker push ghcr.io/siberianmh/edis-bot:${{ github.event.pull_request.head.sha }}
      #############
      # Deploying #
      #############
      - name: Rollout
        run: |
          echo "Usually we don't deploy code, so here we do the same"
